using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This class handles dealing with the ProbePanelSliceShader, which uses a single Texture2D to pass information back and forth between
/// the CPU and GPU about the position 
/// </summary>
public class TP_ProbePanelManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
